{
  "name": "mocha-debug",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --full-app --driver-package=practicalmeteor:mocha --port 9000"

  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
